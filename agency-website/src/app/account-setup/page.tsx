import PortalPaused from '../../components/PortalPaused';

export default function AccountSetupPage() {
  return <PortalPaused title="Account Setup Disabled" description="Invitation-based account setup is paused while the site is being finalized as a marketing-first presence." />;
}
