import PortalPaused from '../../components/PortalPaused';

export default function ResetPasswordPage() {
  return <PortalPaused title="Reset Flow Disabled" description="The account reset flow depended on the retired backend and has been intentionally disconnected from the public site." />;
}
