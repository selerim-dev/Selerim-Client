import PortalPaused from '../../components/PortalPaused';

export default function ForgotPasswordPage() {
  return <PortalPaused title="Password Reset Disabled" description="Password recovery for the old portal is paused while Selerim runs as a frontend-only marketing site." />;
}
