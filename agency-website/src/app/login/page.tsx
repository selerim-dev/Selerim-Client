import PortalPaused from '../../components/PortalPaused';

export default function LoginPage() {
  return <PortalPaused title="Client Login Disabled" description="The legacy client portal is not part of the public launch. Reach out through the contact page if you need private access restored." />;
}
