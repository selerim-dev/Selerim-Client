import PortalPaused from '../../components/PortalPaused';

export default function InvitePage() {
  return <PortalPaused title="Invitation Flow Disabled" description="Client invitation links relied on the old backend. They are intentionally disconnected while the public site is finalized." />;
}
