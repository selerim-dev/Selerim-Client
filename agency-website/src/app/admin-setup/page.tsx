import PortalPaused from '../../components/PortalPaused';

export default function AdminSetupPage() {
  return <PortalPaused title="Admin Setup Disabled" description="The bootstrap flow for the old backend has been disconnected from the public site and is no longer part of launch readiness." />;
}
